# routes as defined in `run.js`

> 
> > /
> > /api/
> > /latest `GET` `POST` `ws`
> > /base `GET` `POST`
> > /* (fallback) TODO: 302 Found serverside redirect, setHeader("Location", "/")
> > /login `GET` `POST`
> > /auth/ `POST`
> 
